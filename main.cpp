#include <iostream>
#include "Booking.h"
using namespace std;

// Room

// Customers

// Bookings

int main(){
    // Rooms
    map<int, Room*> rooms; // Room number as key

    // Customers
    map<string, Room*> customers; // IC as key

    // Bookings
    map<int, Room*> bookings; // id as key

    return 0;
}